- **统一编排模式**：采用 `.qoder/agents` 定义审查角色（如 DAX、模型、性能），`.qoder/skills` 封装领域知识，形成“意图识别-任务拆解-规范校验”的闭环工作流。
- **跨域契约共享**：在 `code_copilot` 与 `powerbi_code_copilot` 间复用 `changes/templates`（spec, tasks, log）及 `rules`（security, domain-rules），确保不同技术栈下的变更管理逻辑一致。
- **知识资产分层**：将通用编程规范与特定领域知识（DAX Patterns, ClickHouse SQL）解耦，通过 `knowledge/index.md` 建立索引，支持 Agent 按需检索并注入上下文。