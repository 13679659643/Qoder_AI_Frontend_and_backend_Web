| **数据类型** | percent_0dp → 百分比整数，不含正号 |
| **数据格式** | `#,##0%` |

| **数据类型** | percent_1dp → 百分比，保留一位小数，不含正号 |
| **数据格式** | `#,##0.0%`|

| **数据类型** | delta_pct_0dp → 百分比整数，含正号：+15% / -3% |
| **数据格式** | IF(__Value > 0, "+", "") & FORMAT(__Value, "#,##0%") |

| **数据类型** |  delta_pct_1dp → 百分比，保留一位小数，含正号：+14.5% / -3.2%|
| **数据格式** | IF(__Value > 0, "+", "") & FORMAT(__Value, "0.0%") |

| **数据类型** | 两种数据格式：currency → 货币符号由币种切片器决定，千分位整数 或者 currency_k  →  货币符号由币种切片器决定，整数以K为单位，¥1k / $5k |
| **数据格式** | `#,##0`（在 DAX 中用 `__CurrencySymbol & FORMAT(__Value, "#,##0")` 拼接币种符号） / __CurrencySymbol & FORMAT(__Value / 1000, "#,##0") & "k"|

| **数据类型** | currency_M_K_Int_0db → 值 < 1,000        → 货币符号 + 千分位整数：¥999;1,000 ≤ 值 < 1M   → 货币符号 + K 单位（1位小数）：¥1.5K;值 ≥ 1,000,000    → 货币符号 + M 单位（1位小数）：¥1.5M; |
| **数据格式** | dax```IF( __Value < 1000,__CurrencySymbol & FORMAT(__Value, "#,##0"),IF(__Value < 1000000,__CurrencySymbol & FORMAT(__Value / 1000, "#,##0.0") & "K",__CurrencySymbol & FORMAT(__Value / 1000000, "#,##0.0") & "M")), // ¥999\¥1.5K\¥1.5M; ```|

| **数据类型** | delta_pts → 增减基点整数： → +120pts / -80pts（基点，含正负号，值×100 转 pts）,乘以100的操作可以放在Cell Display度量中实现，算同比LY：当期值 − 同期值（差值，pts 指标，展示时 ×100 转 pts） |        
| **数据格式** | `+#,##0pts;-#,##0pts;0pts` |

| **数据类型** | integer → 整数，千分位整数 |
| **数据格式** | `#,##0` |